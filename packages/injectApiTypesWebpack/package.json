{
  "name": "@jsjn/inject-api-type",
  "version": "1.0.6",
  "description": "注入接口类型，使得api调用时获取智能提示, 适用于webpack。",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "publish": "npm version patch && npm publish --access public"
  },
  "dependencies": {
    "chokidar": "^4.0.1"
  },
  "devDependencies": {}
}
